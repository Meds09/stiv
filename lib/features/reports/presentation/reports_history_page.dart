import 'package:flutter/material.dart';

class ReportsHistoryPage extends StatelessWidget {
  const ReportsHistoryPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const Center(
        child: Text('HistoryRecordPage'),
      ),
    );
  }
}